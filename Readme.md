## # s21_matrix

📚 Реализация класса матриц на C++ — учебный проект по работе с динамической памятью, перегрузкой операторов и основами линейной алгебры.

## 📌 Описание

Класс `S21Matrix` представляет собой двумерную матрицу вещественных чисел с поддержкой:
- Конструкторов (по умолчанию, параметрический, копирования, перемещения) и деструктора
- Основных операций: сложение, вычитание, умножение на число и на другую матрицу
- Присваивающих операторов (`+=`, `-=`, `*=`) и арифметических (`+`, `-`, `*`)
- Индексации через `operator()(int i, int j)`
- Сравнения (`operator==`)
- Транспонирования, вычисления определителя, матрицы алгебраических дополнений и обратной матрицы

## 🛠 Сборка

Проект собирается с помощью `Makefile`. Примеры команд:

```bash
make all        # сборка библиотеки и тестов
make test       # запуск модульных тестов
make clean      # удаление временных файлов
